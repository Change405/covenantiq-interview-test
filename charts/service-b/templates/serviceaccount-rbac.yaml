{{- if .Values.serviceB.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "service-b.serviceAccountName" . }}
  labels:
    {{- include "service-b.labels" . | nindent 4 }}
{{- end }}

{{- if .Values.serviceB.rbac.enabled }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "service-b.fullname" . }}-role
  labels:
    {{- include "service-b.labels" . | nindent 4 }}
rules:
  # TODO(candidate): add least-privilege rules if the service calls the Kubernetes API.
  # If the service does not need Kubernetes API access, set `.Values.serviceB.rbac.enabled=false`
  # and document the decision in your submission.
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "service-b.fullname" . }}-rolebinding
  labels:
    {{- include "service-b.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: {{ include "service-b.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
roleRef:
  kind: Role
  name: {{ include "service-b.fullname" . }}-role
  apiGroup: rbac.authorization.k8s.io
{{- end }}

