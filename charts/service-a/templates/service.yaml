apiVersion: v1
kind: Service
metadata:
  name: {{ include "service-a.fullname" . }}
  labels:
    {{- include "service-a.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "service-a.labels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.serviceA.containerPort }}
      targetPort: http

