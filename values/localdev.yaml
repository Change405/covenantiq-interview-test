# Needed for: ./scripts/validate_helm_requirements.sh localdev, make deploy-localdev, make smoke-localdev.

serviceA:
  replicaCount: 1
  appEnv: localdev
  config:
    logLevel: debug

serviceB:
  replicaCount: 1
  appEnv: localdev
  config:
    logLevel: debug

worker:
  replicaCount: 1
  appEnv: localdev
  config:
    logLevel: debug