# Needed for: ./scripts/validate_helm_requirements.sh staging, CI, deploy/smoke to staging.

serviceA:
  replicaCount: 2
  appEnv: staging
  config:
    logLevel: info

serviceB:
  replicaCount: 2
  appEnv: staging
  config:
    logLevel: info

worker:
  replicaCount: 1
  appEnv: staging
  config:
    logLevel: info