# Needed for: ./scripts/validate_helm_requirements.sh sandbox, CI, deploy/smoke to sandbox.

serviceA:
  replicaCount: 1
  appEnv: sandbox
  config:
    logLevel: info
  secret:
    appToken: takehome-placeholder

serviceB:
  replicaCount: 1
  appEnv: sandbox
  config:
    logLevel: info
  secret:
    appToken: takehome-placeholder

worker:
  replicaCount: 1
  appEnv: sandbox
