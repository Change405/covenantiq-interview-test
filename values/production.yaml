# Needed for: ./scripts/validate_helm_requirements.sh production, CI, deploy/smoke to production.

serviceA:
  replicaCount: 3
  appEnv: production
  config:
    logLevel: warn

serviceB:
  replicaCount: 3
  appEnv: production
  config:
    logLevel: warn

worker:
  replicaCount: 1
  appEnv: production
  config:
    logLevel: warn